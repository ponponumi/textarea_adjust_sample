{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "sass": "^1.83.0",
    "vite": "^6.0.3"
  },
  "dependencies": {
    "destyle.css": "^4.0.1",
    "sass-mq": "^6.0.0",
    "textarea-auto-height-adjust": "^1.0.0"
  }
}
