{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "sass": "^1.83.0",
    "vite": "^6.0.3"
  },
  "dependencies": {
    "textarea-auto-height-adjust": "^1.0.0"
  }
}
